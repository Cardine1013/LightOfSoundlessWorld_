<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/twice_neon_magenta</item>
        <item name="colorPrimaryDark">@color/twice_neon_magenta</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/twice_dahyun</item>
    </style>

</resources>